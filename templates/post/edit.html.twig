{% extends 'base.html.twig' %}

{% block title %}{{ 'post.edit'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('form') }}
{% endblock %}

{% block body %}
    <div class="box">
        <h1>{{ 'post.edit'|trans }}</h1>
        {{ form_start(form) }}
        {{ form_widget(form)}}
        <input type="submit" name="" value={{ 'u.btn'| trans }}>
        {{ form_end(form) }}
    </div>
{% endblock %}