<h1>{{ 'restore.hi'|trans }}</h1>

<p>
    <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ 'restore.reset'|trans }}</a>
    {{ 'restore.expire'|trans }} {{ tokenLifetime|date('g') }} {{ 'confirm.hours'|trans }}.
</p>

<p>
    {{ 'confirm.cheers'|trans }}
</p>